/**
 * Enumeration of available applications in which permissions can be applied.
 *
 * - `PANEL`: Permissions that apply to the administration panel.
 * - `SHOP`: Permissions that apply to the shop frontend.
 */
export enum EPermissionApps {
	PANEL = 'panel',
	SHOP = 'shop',
}
